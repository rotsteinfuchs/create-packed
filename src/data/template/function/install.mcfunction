# Author: @Rotsteinfuchs
# Description: Commands in this file will run once when the data pack gets installed.
#
# Call: template:load (only once)

# Create Scoreboard(s)

# Create Storage(s)

# Change state to installed
data merge storage pack:template { installed: 1b }
